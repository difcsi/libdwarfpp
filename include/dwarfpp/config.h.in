#define HAVE_DWARF_FRAME_OP3 @HAVE_DWARF_FRAME_OP3@
#define HAVE_GELF_OFFSCN @HAVE_GELF_OFFSCN@
/* 1 to back libdwarfpp with elfutils' libdw (via the compat shim in
 * libdw.hpp); 0 to use David Anderson's libdwarf. */
#define DWARFPP_USE_LIBDW @DWARFPP_USE_LIBDW@
